# Source Code

- `Host` contains the web host and application composition root.
- `BuildingBlocks` contains shared, stable contracts used by multiple modules.
- `Modules` contains independently developed functional and system modules.